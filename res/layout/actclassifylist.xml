<?xml version="1.0" encoding="UTF-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/bg"
    android:orientation="vertical" >

    <RelativeLayout style="@style/topbar" >

        <ImageButton
            android:id="@id/classifyListBack"
            style="@style/topbar_btn"
            android:layout_centerVertical="true"
            android:scaleType="center"
            android:src="@drawable/icon_back" />

        <TextView
            android:id="@id/classifyTitle"
            android:layout_width="wrap_content"
            android:layout_height="fill_parent"
            android:layout_centerInParent="true"
            android:gravity="center"
            android:textColor="#ffffffff"
            android:textSize="18.0sp" />
    </RelativeLayout>

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent" >
         <!--
        <com.nodejs.comic.views.ClassifyView
        	android:id="@+id/classifyList"
        	android:layout_width="fill_parent"
            android:layout_height="fill_parent"/>

       
        <cn.ibuka.manga.ui.ViewMangaList
            android:id="@id/classifyList"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:cacheColorHint="#00000000"
            android:divider="@drawable/list_line"
            android:fadingEdge="none"
            android:listSelector="#00000000"
            android:scrollbarThumbVertical="@drawable/scrollbar"
            android:scrollingCache="false" />
        

        <include layout="@layout/viewdownloadstate" />
        -->
    </RelativeLayout>

</LinearLayout>